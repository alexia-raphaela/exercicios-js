// Quando você tem o operando e o operandor você tem o postfix que vem depois do operando 
// Temos também o pré fix que vem antes do operando
// E temos infix quando é operando 1 + operando 2. De acordo com essas definições por isso o operador é binário
// Quando você tem um operador trabalhando em cima de apenas um operando, é chamado de unário
const [a, b, c, d] = [3, 5, 1, 15]

const soma = a + b + c + d
const subtracao = d - b
const multiplicacao = a * b
const divisao = d / a
const modulo = a % 2

console.log(soma, subtracao, multiplicacao, divisao, modulo)

// O operador aritmético é um operador binario, porque existe dois operando