const sequencia = (numA, numB) => {
    let result = 0;
    let array = []
    for(let i = 0; i < numB; i++) {
        result = numA
        array.push(result)
        
    }
   return array
}
console.log(sequencia(3, 3))