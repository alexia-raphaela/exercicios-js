// Você cria uma referencia a partir do underline
const _= require('lodash')
setInterval(() => console.log(_.random(1, 1000)), 2000) // Função que será executada de tempos em tempos

// -g significa que será isntalada de forma global