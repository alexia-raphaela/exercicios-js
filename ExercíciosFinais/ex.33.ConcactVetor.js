let vetorInteiro = [150, 200, 1500, 2000];
let vetorString = ['mil', 'reais', 'dólares', 'anos'];
let vetorDouble = [0.01, 1.02, 2.04, 3.08];

let concat = vetorInteiro.concat(vetorString, vetorDouble)
console.log(concat)